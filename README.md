
  # CJA Capital Group Website

  This is a code bundle for CJA Capital Group Website.
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
